import pygame as pg
import time

    #pg.mixer.init()
    #song = pg.mixer.music.load("test.MP3")
    #pg.mixer.music.set_volume(0.3)
    #pg.mixer.music.play(-1)
    #while best solution for generic audio files mutagen mp3 would need transformation
    #while(pg.mixer.music.get_busy()):
    #    continue

    #pg.quit()
print("E X I T ")
